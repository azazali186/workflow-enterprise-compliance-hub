# Base URL of the backend API. Leave empty when using the Vite dev proxy
# (/api -> http://localhost:8080). Set to the absolute API origin for
# production deployments served from a different domain.
VITE_API_URL=

# Dev-only: backend target for the /api proxy.
# VITE_PROXY_TARGET=http://localhost:8080
